# Test structure

- `unit`: pure functions, contracts, and state machines.
- `integration`: repository, route, and page boundaries.
- `e2e`: critical browser journeys.
- `fixtures`: deterministic reusable domain objects and repository substitutes.
- `visual`: approved visual baselines and helpers.

Tests never connect to a real Sanity project or include real credentials.
