# Feature modules

Each child directory owns one independently testable product feature. Features consume domain contracts and shared components; they do not access Sanity directly or import another feature's private implementation.
